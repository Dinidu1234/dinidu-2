<UserControl x:Class="KickBlastStudentUI.Views.HistoryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
            <ComboBox Width="220" Height="36" ItemsSource="{Binding Athletes}" SelectedItem="{Binding SelectedAthleteFilter}" DisplayMemberPath="Name" Margin="0,0,8,0" />
            <TextBox Width="100" Height="36" Text="{Binding MonthFilter}" Margin="0,0,8,0" />
            <TextBox Width="120" Height="36" Text="{Binding YearFilter}" />
        </StackPanel>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*" />
                <ColumnDefinition Width="1*" />
            </Grid.ColumnDefinitions>

            <Border Style="{StaticResource CardStyle}" Margin="0,0,10,0">
                <DataGrid ItemsSource="{Binding Calculations}" SelectedItem="{Binding SelectedCalculation}" AutoGenerateColumns="False">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Athlete" Binding="{Binding Athlete.Name}" Width="*" />
                        <DataGridTextColumn Header="Month" Binding="{Binding Month}" Width="*" />
                        <DataGridTextColumn Header="Year" Binding="{Binding Year}" Width="*" />
                        <DataGridTextColumn Header="Total" Binding="{Binding TotalCost}" Width="*" />
                    </DataGrid.Columns>
                </DataGrid>
            </Border>

            <Border Grid.Column="1" Style="{StaticResource CardStyle}">
                <StackPanel>
                    <TextBlock Text="Details" FontSize="16" FontWeight="SemiBold" Margin="0,0,0,8" />
                    <TextBlock Text="{Binding DetailTraining, StringFormat=Training: {0}}" Margin="0,4,0,0" />
                    <TextBlock Text="{Binding DetailCoaching, StringFormat=Coaching: {0}}" Margin="0,4,0,0" />
                    <TextBlock Text="{Binding DetailCompetition, StringFormat=Competition: {0}}" Margin="0,4,0,0" />
                    <TextBlock Text="{Binding DetailTotal, StringFormat=Total: {0}}" Margin="0,4,0,0" FontWeight="Bold" />
                    <TextBlock Text="{Binding DetailWeight, StringFormat=Weight Status: {0}}" Margin="0,8,0,0" />
                    <TextBlock Text="{Binding DetailSecondSaturday, StringFormat=Second Saturday: {0}}" Margin="0,4,0,0" />
                </StackPanel>
            </Border>
        </Grid>
    </Grid>
</UserControl>
